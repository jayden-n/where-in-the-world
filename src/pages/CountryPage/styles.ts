import styled from 'styled-components';

export const CountryPage = styled.main`
  height: calc(100vh - 90px);
  .container {
    width: auto;
    padding: 40px 0px;
  }
  .loading {
    min-height: 100vh;
  }
`;
